{
  "name": "sainome",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:syntax": "node --check js/*.js && node --check scripts/*.js",
    "build:pages": "node scripts/prepare-github-pages.js",
    "verify:pages": "node scripts/verify-github-pages.js"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.4"
  }
}
